- 認証は起動時に毎回要求。client.jsonがある場合にのみ要求し、usertokenはファイルとしては保存しないようにする。
- アプリ側で認証とAPIをたたいての情報取得を行う。基本的には5分ごととかの定期的なタイミングとAI会話を行う直前に行う。
- 取得したカレンダーの情報はjson形式でファイル保存し、アプリの終了時にファイルは削除するもしくは起動時に古いものをすべて削除する。（安全性のため）
- AItools側はjsonファイルを参照してユーザーのカレンダー情報を確認する。


## 20260819
動作確認できたのでコミット。  
日記作成も入れてしまいたかったけど要ログ機能と要約機能を再度調整しないといけなさそうなので一度保留。  


## 20260812
GoogleCalenderのAPIを叩いて予定を取得する機能、それを使って会話する機能を実装。  
とりあえず動いたのは確認できたので細かい動作を確認しつつ問題がなければ過去日についての要約日記作成プログラムを作る。

## それ以前
方針とか要件定義